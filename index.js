// console.log("Hello FizzBazz!");
for (let i = 1; i < 100; i++) {
  console.log((i % 3 == 0 ? "Fizz" : "") + (i % 5 == 0 ? "Buzz" : "") || i);

  // comment for second commit
}



// added some new features 
// some changes made on GitHub
